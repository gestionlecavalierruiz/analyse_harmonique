

def cycle(x):

    a = x[0]
    # Suivre le cercle de I - IV - VII - III - VI - II - V - I

    # Suivre cercle avec le remplacement
    # Suivre cercle avec le rembplacment
    # Regle si valeur de la serie equivaut a IV alors elle ne peut pas avoir autre chose que I avant
    # Regle si valeur de la serie equivaut a VII alors elle ne peut avoir autre chose que IV

    for i in liste:
        if sequence[i] == "IV":
            if sequence[i-1] == "I":
                # on garde
        else:

            #on saute

    return